# Ejemplo de valores para crear el Secret backstage-secret (NO aplicar directamente en GitOps)
# Mantén este archivo fuera de sincronización de ArgoCD (extensión .txt evita parseo como manifiesto Kubernetes).
# Comando sugerido:
# kubectl -n backstage-manual create secret generic backstage-secret \
#   --from-literal=BACKEND_AUTH_KEYS_0_SECRET="$(openssl rand -hex 32)" \
#   --from-literal=GITHUB_CLIENT_ID="<id>" \
#   --from-literal=GITHUB_CLIENT_SECRET="<secret>" \
#   --from-literal=GITHUB_TOKEN="<pat>"
# Opcional: usar SealedSecrets / ExternalSecrets para gestión segura.
