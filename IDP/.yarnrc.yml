nodeLinker: node-modules

# Removed yarnPath to avoid ENOENT when the release file isn't committed. Deterministic version now enforced via
# packageManager field in root package.json and `corepack prepare yarn@4.4.1 --activate` in Dockerfile.
